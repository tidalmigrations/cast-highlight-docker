version: build-{build}.{branch}
image: Visual Studio 2017

environment:
  image: cast-highlight:latest

build_script:
  - cmd: appveyor-retry docker build --pull -t %image% .

after_build:
  - cmd: docker images

test_script:
  - cmd: docker run --rm %image% --printTechnos
